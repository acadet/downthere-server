.upload-form
  %h1 Upload a new text file

  - unless @error.nil?
    %p.error=@error

  = form_for @file, html: {multipart: true } do |f|
    %p
      = f.file_field :attachment, class: 'attachment-button'
    %p.right
      = f.submit "Save", class: 'save-button'
